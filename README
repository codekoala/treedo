TreeDo is a simple Python-based ToDo application.  It stores all of the information in XML format.

Features currently include:

* The ability to arbitrarily nest tasks
* Check boxes to indicate whether or not a task is complete
* Due date/time

More features are definitely planned for the future.

Requirements
============

TreeDo requires the following libraries:

* Python 2.5 or higher
* wxPython 2.8.10.1 or higher
* lxml 2.2.0 or higher

